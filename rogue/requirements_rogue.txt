# -----------------------------------------------------------------------
# Rogue mobile game requirements
# -----------------------------------------------------------------------

# Kivy – cross-platform mobile UI framework
# Install:  pip install kivy
kivy>=2.3.0

# Kivy dependencies (automatically pulled in by kivy, listed for clarity)
# kivy-deps.sdl2    (Windows)
# kivy-deps.glew    (Windows)
# kivy-deps.angle   (Windows, optional)

# For building the Android APK with buildozer:
#   pip install buildozer
#   (then run: buildozer android debug)
#
# For iOS, use kivy-ios:
#   pip install kivy-ios
